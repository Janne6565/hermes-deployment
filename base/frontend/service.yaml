apiVersion: v1
kind: Service
metadata:
  name: hermes-frontend
  labels:
    app.kubernetes.io/name: hermes
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: hermes
    app.kubernetes.io/component: frontend
  ports:
    - name: http
      port: 80
      targetPort: http
