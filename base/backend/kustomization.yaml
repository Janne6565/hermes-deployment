apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  - networkpolicy.yaml
  # SealedSecrets: ciphertext only, decryptable by this cluster's controller alone.
  - sealed-hermes-db.yaml
  - sealed-hermes-app-key.yaml
  - sealed-alert-webhook-secret.yaml
  - sealed-gmail-oauth.yaml
  - sealed-ntfy-credentials.yaml
  - sealed-claude-oauth-token.yaml
